export default ({children}) => (
    <div 
        style={{
            //Layout styling
            margin: `10%`,
            backgroundColor: `#fafafa`
        }}
    >
        {children}
    </div>
)